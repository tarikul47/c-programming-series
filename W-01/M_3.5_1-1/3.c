#include <stdio.h>

int main()
{
    int a, l, w;
    scanf("%d, %d, %d", &a, &l, &w);

  //  total = aythokar_count * length * width;
    printf("%d %d %d", a, l, w);
    //printf("%d", 1 + l);
    return 0;
} 